
<% if artist.followed_by?(current_user)%>
 <%= link_to 'Unfollow', artist_follow_path(artist), method: :post, class: 'btn btn-secondary'%>

<% else %>
 <%= link_to 'Follow', artist_follow_path(artist), method: :post, class: 'btn btn-primary'%>

<% end %>

