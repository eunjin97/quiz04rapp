
<% if song.followed_by?(current_user)%>
 <%= link_to 'Unfollow', song_follow_path(song), method: :post, class: 'btn btn-secondary'%>

<% else %>
 <%= link_to 'Follow', song_follow_path(song), method: :post, class: 'btn btn-primary' %>

<% end %>

